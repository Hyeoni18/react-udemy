// 파일 별로 하나만 만들 수 있는 default, import { apiKey } 가 아닌 이름 지정 필요 -> import key from
// 향후 react 파일은 이 방식을 많이 사용하니 알아두기, 파일에서 export 할 대상이 하나의 컴포넌트 뿐이기에.
export default "adnasdoasflak1";
export let apiKey = "adnasdoasflak1";
export let abc = "abc";
