import { apiKey, abc as content } from "./util.js";
// import 하려는 파일 내 모든 대상을 객체화 후 사용, default 값은 util.default
import * as util from "./util.js";

console.log(util.apiKey);
console.log(apiKey);
console.log(content);